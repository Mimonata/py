[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"
license = "MIT"

[project.urls]
git-repo = "https://github.com/mimonata"

[[project.authors]] 
name = "spitul"
email = "spitul@42.fr"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
